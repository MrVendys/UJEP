<Window x:Class="Canban.View.Windows.GraphWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Canban.View.Windows"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Title="GraphWindow" Height="450" Width="800">
    <Grid>
        <lvc:CartesianChart x:Name="Graph" Series="{Binding SeriesCollection}">
            <lvc:CartesianChart.AxisX>
                <!-- X Axis uses real DateTime values formatted for human readability -->
                <lvc:Axis x:Name="XAxis" Title="Date" LabelFormatter="{Binding DateFormatter}">
                    <lvc:Axis.Separator>
                        <lvc:Separator Step="1" />
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisX>

            <!-- Y Axis represents cycle time in days -->
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Cycle Time (Days)" MinValue="0"/>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
    </Grid>
</Window>
