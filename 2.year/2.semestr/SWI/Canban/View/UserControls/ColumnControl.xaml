<UserControl x:Class="Canban.View.UserControls.ColumnControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Canban.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" MouseMove="UserControl_MouseMove_1">
    <Grid x:Name="TaskControlGrid" Width="200" Height="430" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="1*"/>

        </Grid.RowDefinitions>
        <TextBlock 
            Grid.Row="0" 
            TextAlignment="Center" 
            FontSize="25" 
            Text="{Binding ColumnName}"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto" MinHeight="50"  Grid.Row="1" Margin="5" BorderThickness="10">
            <StackPanel 
                x:Name="TaskStackPanel" 
                MinHeight="50" 
                AllowDrop="True" 
                Background="Aqua"
                MouseMove="TaskStackPanel_MouseMove" 
                Drop="TaskStackPanel_Drop"/>
        </ScrollViewer>
        <Button 
            x:Name="AddTaskBtn" 
            Grid.Row="2" 
            Width="100" 
            HorizontalContentAlignment="Center" 
            FontSize="20" 
            Content="+" 
            Click="AddTaskBtn_Click"/>

    </Grid>

</UserControl>
